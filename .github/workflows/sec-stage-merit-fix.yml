name: Apply SEC stage merit fix
on:
  push:
    paths:
      - .github/workflows/sec-stage-merit-fix.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Validate
        run: echo ok
